import EmailBodyContent from "./EmailBodyContent"

function EmailBody() {
    return (
        <section className="email-body">
            <EmailBodyContent />
        </section>
    )
}
export default EmailBody